---
"@arshad-shah/swift-chart": patch
---

Internal: bump dev tooling to resolve advisories. Updates `canvas` 2→3, `jsdom` 25→29, `vitest` 2→4, playground `vite` 5→8, docs `astro` 5→6 and `@astrojs/starlight` 0.32→0.39. No runtime or API changes — the published bundle is unaffected.
