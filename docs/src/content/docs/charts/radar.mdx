---
title: Radar chart
description: Spider/radar polygon chart for multi-axis comparison.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import ChartPreview from '../../../components/ChartPreview';
import { skills } from '../../../components/ExampleData';

<ChartPreview
  client:visible
  kind="radar"
  data={skills}
  mapping={{ x: 'axis', y: ['a', 'b'], seriesNames: ['Team A', 'Team B'] }}
  options={{ theme: 'midnight' }}
  height={360}
/>

<Tabs syncKey="api">
  <TabItem label="Vanilla JS">
    ```ts
    import { RadarChart } from '@arshad-shah/swift-chart';

    const chart = new RadarChart('#chart', { theme: 'midnight' });
    chart.setData(skills, { x: 'axis', y: ['a', 'b'] });
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { Radar } from '@arshad-shah/swift-chart/react';

    <Radar
      data={skills}
      mapping={{ x: 'axis', y: ['a', 'b'] }}
      theme="midnight"
      height={360}
    />
    ```
  </TabItem>
</Tabs>

## Reference

- Class: [`RadarChart`](/api/core/classes/radarchart/)
- React component: [`Radar`](/api/react/variables/radar/)
