---
title: Marimekko / mosaic chart
description: Variable-width stacked bars — column width and segment height both encode magnitude.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import ChartPreview from '../../../components/ChartPreview';
import { segmentRevenue } from '../../../components/ExampleData';

<ChartPreview
  client:visible
  kind="marimekko"
  data={segmentRevenue}
  mapping={{ x: 'segment', y: ['premium', 'plus', 'free'] }}
  options={{ theme: 'midnight' }}
  height={320}
/>

<Tabs syncKey="api">
  <TabItem label="Vanilla JS">
    ```ts
    import { MarimekkoChart } from '@arshad-shah/swift-chart';

    new MarimekkoChart('#chart', { theme: 'midnight' })
      .setData(segmentRevenue, { x: 'segment', y: ['premium', 'plus', 'free'] });
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { Marimekko } from '@arshad-shah/swift-chart/react';

    <Marimekko
      data={segmentRevenue}
      mapping={{ x: 'segment', y: ['premium', 'plus', 'free'] }}
      theme="midnight"
      height={320}
    />
    ```
  </TabItem>
</Tabs>

## Reference

- Class: [`MarimekkoChart`](/api/core/classes/marimekkochart/)
- React component: [`Marimekko`](/api/react/variables/marimekko/)
